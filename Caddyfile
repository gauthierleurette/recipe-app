yourdomain.com {
    # Serve uploaded images directly from disk (fast, no Node overhead)
    handle /uploads/* {
        root * /srv
        file_server
    }

    # Everything else goes to Next.js
    reverse_proxy app:3000
}
